# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

# Local databases and caches (regenerable, sometimes large)
*.sqlite
*.db
cache/
knowledge/

# Investigation output
reports/

# Generated intelligence feeds (large; run `ariadne update-intel` to rebuild)
ariadne/data/intel_labels.json
ariadne/data/ofac_sanctioned.json

# Secrets — never commit
*.token
.env
audit*.jsonl
# Ed25519 signing keys — the private key must NEVER be committed
keys/
*.key
ariadne.config.json

# Local editor/preview config
.claude/
